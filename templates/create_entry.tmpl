{{template "header" .}}
<div class="wl-container">
  <div class="wl-header">
    <a class="wl-header-back-button" href="{{.PreviousUrl}}"><img src="/img/ic_back.svg" alt="Zurück"></a>
    <h1>Erstellen</h1>
  </div>
  {{if ne .ErrorMessage "" -}}
  <p class="alert alert-danger">{{.ErrorMessage}}</p>
  {{end -}}
  <form action="/create" method="POST">
    {{template "entry_form" .}}
    <div class="wl-form-buttons">
      <a class="btn btn-light wl-form-button" href="{{.PreviousUrl}}">Abbrechen</a>
      <button class="btn btn-primary wl-form-button" type="submit">Speichern</button>
    </div>
  </form>
</div>
{{template "footer" .}}